{
  "name": "dcy-crypto-backend",
  "version": "0.0.0",
  "private": true,
  "main": "app.js",
  "scripts": {
    "start": "node ./bin/www",
    "dev": "NODE_ENV=development npx functions-framework --target=dcy",
    "staging": "NODE_ENV=staging npx functions-framework --target=dcy",
    "prod": "NODE_ENV=production npx functions-framework --target=dcy",
    "deploy: staging": "NODE_ENV=staging gcloud app deploy",
    "deploy: prod": "NODE_ENV=production gcloud app deploy"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "dotenv": "^16.0.3",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.2.0",
    "jest": "^29.5.0",
    "supertest": "^6.3.3"
  }
}
